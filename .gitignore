# ── OS / Editor ───────────────────────────────────
.DS_Store
.idea/
.fleet/
*.log
logs

# ── Git worktrees (isolated theme workspaces) ─────
.worktrees/

# ── Environment files ─────────────────────────────
.env
.env.*
!.env.example

# ── Eval harness run outputs (dataset stays committed) ──
evals/results/

# ── Nuxt dev/build outputs ────────────────────────
node_modules/
.output
.vercel
.data
.nuxt
.nitro
.cache
dist
